# Core Flask dependencies
flask==2.3.3
gunicorn==21.2.0

# LINE Bot SDK - 使用最新相容版本
line-bot-sdk>=3.4.0

# HTTP requests
requests>=2.31.0

# aiohttp - 使用較新版本避免編譯問題且有預編譯 wheels
aiohttp>=3.12.0

# Web scraping - 使用預編譯版本
beautifulsoup4>=4.12.2
lxml>=4.9.3

# RSS parsing
feedparser>=6.0.10

# Google AI - 使用較新版本避免 aiohttp 衝突
google-generativeai>=0.7.0

# Environment management
python-dotenv>=1.0.0

# 相容性約束 - 放寬版本限制
urllib3>=1.26.0
certifi>=2023.7.22
